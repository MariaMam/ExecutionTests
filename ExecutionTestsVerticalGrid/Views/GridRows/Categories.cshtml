@model IEnumerable
@{ DemoHelper.Instance.ControlAreaMaxWidth = Unit.Pixel(1046); }
@section StartDemoRegion {
    @{ Html.BeginForm(); }
}

@Html.Partial("CategoriesPartial", Model)

@section DemoOptions {
    @Html.Partial("CategoriesOptionsPartial", CategoriesDemoHelper.Options)
}
@section EndDemoRegion {
    @{ Html.EndForm(); }
}